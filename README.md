# Scalable API Gateway

A FastAPI-based API gateway with JWT authentication, Redis response
caching, per-client rate limiting, and a simple circuit breaker, routing
requests to two mock backend services (`user`, `order`).

Pre-alpha: both backend services return static placeholder data, and
several pieces described below (auth checks, config) have known gaps —
see [`CHANGELOG.md`](CHANGELOG.md).

## Architecture

```
Client
  │
  ▼
FastAPI app (gateway/main.py)
  ├─ log_requests       (gateway/middleware/logging.py)
  ├─ cache_middleware    (gateway/middleware/cache.py, Redis-backed)
  ├─ jwt_auth            (gateway/middleware/auth.py)
  └─ rate_limiter        (gateway/middleware/rate_limit.py)
  │
  ▼
router.py — round-robin + circuit breaker (services/circuit_breaker.py)
  │
  ├─ user_service   (services/user_service.py)
  ├─ user_service_2 (services/user_service_2.py)  — second instance, load-balanced
  └─ order_service  (services/order_service.py)
```

Middleware execution order is the reverse of registration order in
`gateway/main.py` (a Starlette behavior) — see `docs/` for details once
written. [TODO]

## Getting started

### Requirements

- Python 3.13 (matches the checked-in `venv`; not tested on other versions)
- Redis reachable at `localhost:6379`

### Run locally

```bash
python -m venv venv
source venv/bin/activate   # venv\Scripts\activate on Windows
pip install -r requirements.txt

# In separate terminals:
uvicorn gateway.main:app --port 8000
uvicorn services.user_service:app --port 8001
uvicorn services.order_service:app --port 8002
uvicorn services.user_service_2:app --port 8003
```

### Run with Docker Compose

```bash
docker compose up --build
```

Brings up Redis, the gateway, and all three mock services. Routing still
relies on `localhost:<port>` (see `gateway/config.py`), so every service
runs with `network_mode: host` rather than a Docker bridge network —
works on Linux; see the comment in `docker-compose.yml` for the Docker
Desktop caveat.

### Configuration

Every value currently hardcoded in the app (JWT secret, Redis host, rate
limits, etc.) is listed in [`.env.example`](.env.example) for reference.
**None of it is read from the environment yet** — changing `.env` has no
effect until a settings module is added.

## Development

```bash
pip install -r requirements-dev.txt
pre-commit install
```

See [`CONTRIBUTING.md`](CONTRIBUTING.md) for coding standards and the
test workflow.

## API

- `GET /` — health check
- `POST /login` — issues a JWT; does not check any credentials (accepts
  no body, always returns a valid `admin` token)
- `{METHOD} /{service}/{path}` — proxied to the matching upstream service
  in `gateway/config.py`

Full request/response schema: `/docs` (FastAPI's autogenerated Swagger
UI) when the gateway is running.

## Project layout

```
gateway/    API gateway app, middleware, router, config
services/   Mock upstream services + circuit breaker
docker/     Supporting Docker assets (see docker/README.md)
docs/       Project documentation
tests/      Test suite
assets/     Diagrams, images referenced from docs
```

## License

[MIT](LICENSE)
